currently only windows supported

use "git clone --recursive" onley if you dont have wxWidgets installed,
because wxWidgets is freaking huge. it will save you time using what you have installed.

  
  
