An attempt to simulate the path a laser ray takes inside a room filled with mirrors.
- dependent on wxWidgets, glew and glm.
- use "git clone --recursive" onley if you dont have wxWidgets installed,
  because wxWidgets is freaking huge. 
  it will save you time using what you have installed.
  
  